import TYPES from 'types/genres'

const initialState = {
  loading: false,
  data: []
}

const genresReducer = (state = initialState, {type, payload = null}) => {
  switch(type) {
    default: 
      return state
  }
}

export default genresReducer
