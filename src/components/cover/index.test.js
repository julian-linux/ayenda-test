import React from 'react'
import { shallow } from 'enzyme'
import Cover from '.'

describe('Cover Component', () => {
  it('renders without crashing', () => {
    shallow(<Cover />)
  })
  it('shold have class "artist-component-name" if isAlbum prop')
})
